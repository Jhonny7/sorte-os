import "./search.scss"
import { useSearchPresenter } from "./SearchPresenter";

export default function Search({}){

    const {} = useSearchPresenter();

    return <section className="search">
        busqueda
    </section>
}